#!/usr/bin/env bash
# DonaNet — dataset preparation script
# Profile : {{ profile_name }}
# Generated by configure.py — do NOT edit manually.
#
# Usage: ./prepare.sh [donanet prepare-dataset flags]
#
# Examples:
#   ./prepare.sh --source /path/to/raw --train 0.7 --val 0.2 --test 0.1
#   ./prepare.sh --help
#
# Note: the source path must be accessible from inside the container.
#       Mount additional directories by editing docker-compose.yml if needed.

cd "{{ profile_dir }}"

_CMD="{{ compose_cmd }}"

COMPOSE_DISABLE_ENV_FILE=1 $_CMD run --rm donanet prepare-dataset "$@"
