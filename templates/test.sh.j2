#!/usr/bin/env bash
# DonaNet — inference / evaluation script
# Profile : {{ profile_name }}
# Generated by configure.py — do NOT edit manually.
#
# Usage: ./test.sh [donanet test flags]
#
# Examples:
#   ./test.sh --weights weights/my_run/best.pt
#   ./test.sh --weights weights/my_run/best.pt --conf 0.25
#   ./test.sh --weights weights/my_run/best.pt --save-images
#   ./test.sh --help

cd "{{ profile_dir }}"

_CMD="{{ compose_cmd }}"

COMPOSE_DISABLE_ENV_FILE=1 $_CMD run --rm donanet test "$@"
